#include <custom.h>
#include <sys/defs.h>
#include <sys/printf.h>

void free(void * ptr) {

	/*int a;
	if(size%8 !=0) {
		a = 8;
	}
	a = (size/8)*8;

	void * temp = (void *)(heap_ptr);
	heap_ptr = heap_ptr + a;
	if (heap_ptr > HEAP_END) {
		printf("Couldn't allocate request: Ran out of Heap memory");
                return NULL;
	}
	return temp;
*/
}
